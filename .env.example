# Copy to .env and fill in. The key is read from the environment; never commit a real key.
# Used by the live Anthropic provider (Split 04+); the engine runs fully offline without it.
ANTHROPIC_API_KEY=
